
Набор данных состоит из диагностически размеченных изображений с дополнительной метаинформацией. Изображения представлены в формате JPEG. Связанный файл .csv содержит следующие данные:

Бинарная метка диагностики (target): 1 — злокачественное образование, 0 — доброкачественное.
Потенциальные входные переменные: например, age_approx (примерный возраст), sex (пол), anatom_site_general (область тела).
Дополнительные атрибуты: источник изображения и точный диагноз.
Главная задача задача — отличить доброкачественные случаи от злокачественных. 
Для каждого изображения (isic_id) вы должны предсказать вероятность того, что случай является злокачественным (целевое значение target в диапазоне [0, 1]).

Набор данных SLICE-3D — обрезанные изображения кожных поражений для выявления рака кожи
Для имитации недермоскопических изображений в этом соревновании используются стандартизированные обрезанные изображения поражений, полученные из 3D Total Body Photography (TBP).
Vectra WB360, продукт компании Canfield Scientific, создает 3D-ТБП, которое охватывает всю видимую поверхность кожи в одном макрокачественном снимке. Затем ИИ-алгоритм идентифицирует отдельные поражения на изображении. Это позволяет выделять и экспортировать каждое поражение в виде отдельного фото размером 15x15 мм.

Набор данных включает изображения всех кожных поражений из выборки тысяч пациентов, наблюдаемых в период с 2015 по 2024 год в девяти учреждениях на трех континентах.

Файлы
train-image/ — файлы изображений для обучающего набора (только для обучения).
train-image.hdf5 — изображения из обучающего набора в одном файле HDF5 с isic_id в качестве ключа.
train-metadata.csv — метаданные для обучающего набора.
test-image.hdf5 — изображения тестового набора в одном файле HDF5 с isic_id в качестве ключа. Содержит 3 тестовых примера для проверки работоспособности вашего пайплайна. При проверке отправленной ноутбуки этот файл заменяется на полный скрытый тестовый набор (~500 тыс. изображений).
test-metadata.csv — метаданные для тестового поднабора.
sample_submission.csv — пример файла отправки в корректном формате.

Такие файлы, как train-image, train-image.hdf5 и train-metadata.csv отсутствуют из-за слишком большого веса файлов — их вы сможете найти на сайте kaggle-соревнования.

Ссылка на датасет в kaggle: https://www.kaggle.com/competitions/isic-2024-challenge/data?select=train-image
